import leafImage from "@/assets/leaf.png";

export const coffeeLeavesConfig = {
  count: 100,

  leafImage: leafImage,

  minSize: 20,
  maxSize: 50,

  minDuration: 10,
  maxDuration: 25,

  opacity: 0.7,
};
